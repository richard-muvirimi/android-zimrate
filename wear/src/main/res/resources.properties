# The locale the unqualified values/ folder is written in. Required by
# generateLocaleConfig, which reads it as the default and derives the rest of the
# supported set from the values-* folders present.
unqualifiedResLocale=en-US
